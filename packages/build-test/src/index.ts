import { updateObj, Cookie } from '@mxssfd/ts-utils';

console.log(updateObj({ a: 1 }, { a: 2, b: 3 }));

console.log(Cookie.get('111'));

// const rp = new RandomPicker.RandomPicker([1, 2]);

// console.log(rp.pick());
